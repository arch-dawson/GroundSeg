from dip.ui import Application, Form, SpinBox, Dialog

"""
app = Application()
model = dict(name='', age=0)
view_factory = Dialog('name', SpinBox('age', suffix=" years"), window_title="Extended Model")
view = view_factory(model)
view.visible = True
view.execute()

print("Name:", model['name'])
print("Age:", model['age'])
"""

layp