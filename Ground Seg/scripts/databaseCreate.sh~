#!/bin/bash 

read -r -p "This action will DELETE the fakeTelemetry table. Are you sure? [y/n]" response


if [[ $response =~ ^([yY][eE][sS]|[yY])$ ]] ;
then
    echo success
    #mysql -u root -pP0l@r3ubE -e "DROP TABLE fakeTelemetry IF EXISTS"

    #mysql -u root -pP0l@r3ubE -e "CREATE TABLE fakeTelemetry"
else
    exit 1
fi

